spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:8084
      username: root
      password: s3cr3t

server:
  port: 8761

#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registerWithEureka: false
#    fetchRegistry: false